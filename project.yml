name: WeekPact
options:
  bundleIdPrefix: com.pandeydeep9
  deploymentTarget:
    macOS: "13.0"
packages:
  WeekPact:
    path: .
targets:
  WeekPactMac:
    type: application
    platform: macOS
    sources: [Sources/WeekPactApp]
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.pandeydeep9.WeekPact
    dependencies:
      - package: WeekPact
        product: WeekPactCore
      - target: WeekPactFilter
        embed: true
    info:
      path: MacApp/Info.plist
      properties:
        CFBundleDisplayName: WeekPact
        LSMinimumSystemVersion: "13.0"
        NSAppleEventsUsageDescription: WeekPact measures foreground browser time and redirects a selected site when its daily allowance expires.
    entitlements:
      path: MacApp/WeekPactMac.entitlements
      properties:
        com.apple.developer.system-extension.install: true
        com.apple.developer.networking.networkextension:
          - content-filter-provider-systemextension
        com.apple.security.application-groups:
          - group.com.pandeydeep9.WeekPact
  WeekPactFilter:
    type: system-extension
    platform: macOS
    sources: [Sources/WeekPactFilterPrototype, FilterExtension/main.swift]
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.pandeydeep9.WeekPact.Filter
      PRODUCT_NAME: com.pandeydeep9.WeekPact.Filter
    dependencies:
      - package: WeekPact
        product: WeekPactCore
    info:
      path: FilterExtension/Info.plist
      properties:
        NetworkExtension:
          NEProviderClasses:
            com.apple.networkextension.filter-data: $(PRODUCT_MODULE_NAME).WeekPactDataFilter
    entitlements:
      path: FilterExtension/WeekPactFilter.entitlements
      properties:
        com.apple.developer.networking.networkextension:
          - content-filter-provider-systemextension
        com.apple.security.application-groups:
          - group.com.pandeydeep9.WeekPact
